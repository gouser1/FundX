import { makeStyles } from "@material-ui/styles";

export default makeStyles(() => ({
  appBar: {
    borderRadius: 15,
    margin: "0px 0",
    display: "flex",
    flexDirection: "row",
    justifyContent: "center",
  },
}));
